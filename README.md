# Cisco-Interface-Telnet-Config-Automator
A lightweight, asynchronous Python script that automates basic configuration on Cisco IOS devices (routers/switches) using Telnet. The script logs into a device, enters privileged and configuration mode, and applies IP address configuration on a specified interface (FastEthernet0/1).
